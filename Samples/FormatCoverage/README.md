# Format coverage samples

These fixtures are safe, static Finder samples for Config, Table, XML, and Notebook Preview. They exercise comments, nested config, JSON5 syntax, quoted CSV newlines, XML CDATA/entities, and notebook Markdown/code/text output with unsafe HTML deliberately present as a blocked-output case.
